openai>=1.50.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
langgraph>=1.1,<2.0
langchain-openai>=1.2,<2.0
langchain-core>=1.3,<2.0
mcp>=1.26.0,<2.0
# No OTEL or traceloop dependency on purpose. Tracing is supplied by Agent
# Manager's auto-instrumentation, which injects its own instrumentation stack at
# deploy time. Pinning one here would fight the platform's resolution and
# double-instrument the process.
